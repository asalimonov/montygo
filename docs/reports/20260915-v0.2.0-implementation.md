# v0.2.0 target implementation progress

Objective: implement `docs/brainstorms/20260915-second-stage-api-feedback-target.md`
on the current branch and pass all required tests/checks. Completed against
baseline `08b27a0` on 2026-09-15.

## Components

- [x] Checkout lease: exactly one normal release or termination owner; force retirement independent of protocol mutex.
- [x] Retirement retains capacity until observed worker exit; observer close waits for active use.
- [x] Internal regression tests: idle termination, stale lease after worker reuse, blocked Print, 1000 release/terminate races, delayed observed exit.
- [x] Host parameter labels, positional-only stubs, keyword validation, O(1) lookup.
- [x] Tests/examples/docs for host changes and explicit Record conversion guidance.
- [x] Durable execution admission and ErrSessionBusy; synchronous Go registration; WaitContext.
- [x] Interrupt options/outcomes, execution-owned watchdog, first reason/shorter grace, canonical force fencing.
- [x] Snapshot identity/sequence, context lifetime, restore and control-turn ownership.
- [x] Per-execution/call-ID future subscriptions and cancellable derived conversion.
- [x] Mount handling split and every suspension/resume cancellation boundary.
- [x] Canonical session errors, graceful bounded Close, CloseNow retirement, idle watcher and checkout/shutdown race.
- [x] All target acceptance regressions, including 1000 immediate-interrupt iterations on each backend under -race.
- [x] README, architecture, parity, changelog, examples, and reviewed public API golden.

## Verification

- [x] `GOTOOLCHAIN=local go vet ./...`.
- [x] `GOTOOLCHAIN=local go test -count=1 -timeout=8m ./...` (root package 21.278 s).
- [x] `GOTOOLCHAIN=local go test -race -count=1 -timeout=12m ./...` (root package 85.176 s).
- [x] `golangci-lint run ./...` (0 issues).
- [x] Root/examples/network `go mod tidy -diff`; network `go vet ./...`.
- [x] `make check-pins` and `make test-scripts`.
- [x] `make examples` across native and wasm examples.
- [x] `make server-check`: clippy, 25 unit tests, and 9 session tests.
- [x] `make docker-build`: linux/amd64 and linux/arm64 server image.
- [x] `make docker-build-pyclient`: host-architecture Python client test image.
- [x] `make test-docker` (11.819 s): configured WebSocket root suite.
- [x] Explicit configured WebSocket root suite under `go test -race` (34.898 s).
- [x] `make test-network` (23.568 s), including active ShutdownDump preservation.
- [x] Requirement-by-requirement audit against all 12 lifecycle regressions and host/stub acceptance.

## Acceptance evidence

- Immediate Go-to-Interrupt runs 1000 iterations per backend under the race detector and proves reuse after cooperative outcomes.
- Deterministic lifecycle tests cover reservation, preparation, first-send commitment, callback return, conversion, stale steps/watchdogs, completion/force ordering, and LoadSnapshot cancellation/preparation.
- Snapshot tests cover Busy without cursor consumption, stale old snapshots, cancellation before claim, paused function/name/OS boundaries, and dump ownership.
- Future tests cover call-ID accounting, shared pointers and sessions, failed gathers, snapshot-step lifetime, derived cancellation/panic, cooperative abort, and all terminal cleanup classes.
- Lease tests cover idle termination, stale leases, blocked Print, 1000 release/terminate races, delayed exit accounting, observer lifetime, and spawn publication racing shutdown.
- Terminal-path tests distinguish deliberate close, forced interruption, cancellation, idle crash, disconnect, and active server drain.

## Environment / continuity

Baseline is `08b27a0`; the two target brainstorm files were already untracked
before implementation. Do not discard them. No unrelated dirty files were found.
Use `GOTOOLCHAIN=local`. Docker 29.6.1 and golangci-lint are available.
Resolve the 26.5 macOS SDK via `xcrun`, as the Makefile does; the assumed
`/Applications/Xcode.app` SDK directory does not exist.

`Checkout.KillWorker` was removed. `HoldObserver` is retained by the complete
session execution/control owner so callbacks between protocol turns keep the
observer alive without retaining active worker capacity after force retirement.
